# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# wrangler / cloudflare artifacts
.wrangler
.dev.vars
dist
.vercel/output/static

# wrangler.toml is gitignored because ALL secrets live in the
# Cloudflare Dashboard. Use wrangler.example.toml as the template.
# The real wrangler.toml only has SITE_NAME so it is mostly safe,
# but we keep it out of git as a permanent security best practice.
wrangler.toml

# scratch / temp directories
scratch/
